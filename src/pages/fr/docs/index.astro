---
import Docs from '../../../layouts/Docs.astro'
---

<Docs
	title="Documentation"
	lang="en"
	thumbnail="/documentation/backgrounds/martin-marek-1hRH-z4nvxE-unsplash.jpg"
>
	<section id="summary">
		<!-- this should probably be generated automatically but it will do for now -->
		<a href="/fr/docs/getstarted">
			<div class="left">🏁</div>

			<div class="right">
				<h3>Démarrer</h3>
				<p>Installez Bonjourr et apprenez comment vous pouvez y participer</p>
			</div>
		</a>

		<a href="/fr/docs/overview">
			<div class="left">💻</div>

			<div class="right">
				<h3>Liste de fonctionnalités</h3>
				<p>Apprenez à utiliser les fonctionnalités de Bonjourr via conseils et astuces</p>
			</div>
		</a>

		<a href="/fr/docs/profiles">
			<div class="left">⚙️</div>

			<div class="right">
				<h3>Profiles personnalisés</h3>
				<p>Des thèmes pré-faits pour une personnalisation rapide</p>
			</div>
		</a>

		<a href="/fr/docs/styles">
			<div class="left">✨</div>

			<div class="right">
				<h3>CSS snippets</h3>
				<p>Des extraits de code à copier coller pour personnaliser Bonjourr</p>
			</div>
		</a>
	</section>

	<style>
		#summary {
			margin-top: 6em;
			display: grid;
			grid-template-columns: 1fr 1fr;
			grid-gap: 1em;
		}

		#summary > a {
			display: flex;
			gap: 1.5em;
			padding: 1em;
			border-radius: 15px;
			cursor: pointer;
			border: 1px solid rgba(255, 255, 255, 0);
			color: unset;
		}

		#summary > a:hover {
			text-decoration: none;
		}

		#summary > a:hover {
			background: #252525;
			border: 1px solid rgba(255, 255, 255, 0.06);
		}

		@media (prefers-color-scheme: light) {
			#summary > a:hover {
				background: #e9e9e9;
			}
		}

		#summary > a .left {
			display: flex;
			justify-content: center;
			align-items: center;
			flex-direction: column;
		}

		#summary > a .right {
			display: flex;
			flex-direction: column;
			justify-content: center;
		}

		#summary > a .left {
			font-size: 2.5em;
		}

		#summary h3,
		#summary img {
			padding: 0;
			margin: 0;
		}

		#summary p {
			margin-top: 8px;
			margin-bottom: 0;
			font-size: 0.9em;
			opacity: 0.9;
		}

		@media only screen and (max-width: 700px) {
			#summary {
				grid-template-columns: 1fr;
				margin-top: 4em;
			}
		}
	</style>
</Docs>
